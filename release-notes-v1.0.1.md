# 萌卡用户系统插件 v1.0.1

本版本重点修复远程框架连接配置，并重新设计管理端概览数据卡片。

## 核心更新

- 保存远程框架 WebSocket 主机时同步推导框架 HTTPS 管理地址，避免 WebSocket 已配置但管理 API 与 SSO 仍指向本机。
- 服务令牌输入自动清理首尾空格，减少复制令牌后认证失败的问题。
- 修复插件在 WebSocket 尚未完成握手时停止或重启可能触发未捕获错误、导致进程退出的问题。
- 框架断线重连增加连接状态保护与指数退避，认证成功后自动恢复正常重连间隔。
- 管理端概览移除容易被误解为日期进度条的图形，改为用户新增、日均增长、成交订单、客单价和今日流水等语义明确的摘要卡片。
- 保留 1.0.0 的用户/管理端隔离、双协议权益、等级加速、支付幂等和 SQLite WAL 机制。

## 安装与升级说明

本插件建议配合萌卡 NT 1.9.7 或更高版本使用。升级前停止插件并完整备份 `data` 目录与 `config.yaml`，解压新包后保留原有配置、数据库及 `data/master.key`。

升级后在“插件设置”中重新保存一次框架 WebSocket 地址、端口和令牌；远程主机会自动使用对应 HTTPS 管理地址。本机框架仍保留原有本地 HTTP 管理地址。

外发包不包含用户数据、订单数据、支付配置、框架登录信息、日志、源码、密钥或 Git 元数据。

# 萌卡用户系统插件 v1.0.2

本版本重点统一账号管理与框架前端的字段、样式和操作方式，并将等级加速入口收纳到具体账号中。

## 核心更新

- 账号列表与萌卡 NT 框架保持一致，完整展示头像、昵称、等级、QQ 号、协议、节点、消息统计、在线会话、运行环境和最近信息。
- 账号操作改为框架同款登录/离线按钮与“更多”菜单，移除不一致的自定义管理弹窗。
- 增加独立的离线和删除确认窗口，危险操作对象及影响范围更清晰。
- 增加账号近期日志抽屉，并将框架返回的结构化日志转换为可直接阅读的时间线内容。
- 等级加速不再占用固定导航入口，改为从每个 Android 账号的“更多”菜单进入，并自动选择当前账号刷新任务状态。
- Linux 账号明确标注暂不支持等级加速，避免进入后出现不可用页面。
- 添加账号时按框架返回内容完整提供手机、平板和 Linux 协议，弹窗尺寸同步框架前端比例。
- 登录账号时先检查并复用框架缓存；只有框架明确判定缓存失效后才回到首次登录，网络或签名错误不会触发重复登录。
- Linux 首次登录、二维码生成、1.5 秒状态轮询、手机确认、上线完成及取消操作全部复用框架 1.9.8 的原生登录状态机。
- 重做等级状态摘要，修复圆环、图标和文字在不同尺寸下重叠遮挡的问题。
- 修复消息数为零等边界数据可能造成的前端格式化异常。

## 安装与升级说明

本插件需要配合萌卡 NT 1.9.8 或更高版本使用。升级前停止插件并完整备份 `data` 目录与 `config.yaml`，解压新包后保留原有配置、数据库及 `data/master.key`。

如需管理全部节点的账号，请在框架服务列表中为插件开启“系统管理插件”，并确保插件设置中的 WebSocket 地址、端口和服务令牌与框架一致。

外发包不包含用户数据、订单数据、支付配置、框架登录信息、日志、源码、密钥或 Git 元数据。

# 萌卡用户系统插件 v1.0.3

本版本补齐添加账号所需的设备指纹选择，并将账号安全验证界面与处理链路统一到萌卡 NT 1.9.9 框架前端。

## 核心更新

- 添加账号弹窗新增必填的“设备指纹”栏位，直接读取框架中的设备指纹列表，并展示指纹名称、品牌和机型。
- 不再由插件后台偷偷选择或创建默认指纹。没有可用指纹时会明确提示先在框架创建；提交前和服务端都会校验指纹 ID，避免出现“设备指纹ID不能为空”或账号绑定到错误设备的问题。
- 账号进入安全验证时改用框架同款“选择验证方式”底部面板，完整提供“接收短信”“发送短信”“扫码验证”“其他方式”和“取消登录”。
- 接入框架当前的滑块验证、身份验证、短信验证、安全二维码、状态轮询、重新验证及停止登录 action，不再使用插件自编的简化验证页。
- 安全验证页面保持框架原有信息层级、按钮顺序、二维码布局和移动端交互；验证过程中保留登录会话，取消时由框架统一停止当前登录任务。
- 修复少数不含成功状态码的 SSO/登录响应被前端误判为登录成功的问题。
- 补充框架原生滑块与身份验证承载页，验证跳转不再依赖外部页面样式。

## 安装与升级说明

本插件需要配合萌卡 NT 1.9.9 或更高版本使用。正式版 1.0.2 可直接升级到 1.0.3：先停止插件，完整备份 `config.yaml` 和 `data` 目录，替换程序文件后重新启动，原数据库和配置可继续使用。

如果仍在使用 1.0.2 之前的早期测试数据库，请先按 v1.0.2 的说明完成数据库切换。1.0.3 不包含旧版 API、旧表或旧验证流程的兼容分支。

请确认框架服务列表已为插件开启“系统管理插件”，并保证插件配置中的 WebSocket 地址、端口、服务令牌和服务 ID 与框架当前服务一致。添加账号前还需要至少创建一个可用的设备指纹。

外发包不包含用户数据、订单数据、支付配置、框架登录信息、日志、源码、`dist`、`node_modules`、独立运行环境、密钥或 Git 元数据。

# 萌卡用户系统插件 v1.0.4

本版本以萌卡 NT 2.0.1 当前管理 API 为唯一后端契约，统一账号、验证、等级任务和移动端界面，并重新收紧外发包的源码防泄漏边界。

## 核心更新

- 账号列表、添加、编辑、删除、登录、安全验证、协议与设备指纹字段按框架前端当前流程实现；新增账号不再擅自立即登录。
- 管理员可为框架中的账号分配插件用户归属；用户扫码验证 QQ 本人后，会一次找回该 QQ 的全部协议账号。
- 扫码找回只完成账号所有权验证，不执行完整协议登录、不修改密码、不生成登录缓存，也不干扰已在线账号。
- 等级任务页面复用框架前端的任务结构、图标、状态、补挂和计划交互，并继续通过管理 API 读取和执行任务。
- 登录安全验证使用框架同款验证方式面板和滑块、短信、安全二维码链路，取消时由框架停止当前登录任务。
- 修复外网访问时 SSO 回跳到 `127.0.0.1`、普通用户显示管理员、扫码二维码无法生成及加载状态不居中的问题。
- 商品说明保留换行；桌面弹窗比例、移动端按钮、二维码布局和窄屏文字流向统一到框架设计系统。
- 管理端新增注册开关和注册赠送余额设置，并保持支付、邮件等敏感配置加密存储。

## 不兼容调整

- 必须使用萌卡 NT 2.0.1。插件要求 `management_api_version: 1`，版本不匹配会拒绝进入已连接状态。
- 服务配置不再使用 `system_management`、`allowed_actions` 或旧 action 别名；不会降级调用旧管理接口。
- 管理员 SSO 必须经过框架路径验证框架管理员身份，插件不会直接使用官网用户系统建立管理员会话。

## 安装与升级

从 v1.0.3 升级前，先停止插件并完整备份 `config.yaml` 与 `data` 目录。替换程序文件后保留原配置和数据库，再启动 v1.0.4。请先将框架升级到 v2.0.1。

外发包只提供经过压缩、去符号并封装为 V8 字节码单体程序的运行文件。包内不包含 TypeScript/Vue 源码、源码映射、`src`、`dist`、`node_modules`、独立 Node.js 环境、数据库、运行配置、日志、密钥或 Git 元数据。

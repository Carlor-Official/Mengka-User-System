# 萌卡用户系统插件 v1.0.6

本版本配合萌卡 NT v2.0.3，完成插件服务与账号登录节点解耦，并修复部分管理页面首次进入时显示空白的问题。

## 修复与调整

- 修复账号、设备指纹和节点页面初始化阶段因账号派生状态执行顺序错误而出现空白页的问题。
- 接入框架 v2.0.3 的实例级账号路由：插件服务不再配置或提交 `node_id`，账号 API 由框架按照 `self_id + client_type` 使用账号自身登录节点。
- 测试与部署初始化数据删除插件服务级 `node_id`，防止新环境继续生成已经失效的节点绑定。
- 外发包继续使用生产压缩、源码映射禁用和 V8 字节码单体封装，并在三平台打包后执行源码防泄漏审计。

## 不兼容调整

- 必须配合萌卡 NT v2.0.3 使用。
- 插件服务创建、编辑和初始化 SQL 不再包含 `node_id`。
- 不再读取反向 WS 的 `X-Mengka-Node-ID`，也不再从 ready 消息或 `get_plugin_context` 读取服务级 `node_id`。
- `get_bot_list({ all_nodes: true })` 已改为无参数的 `get_bot_list()`；旧参数不会被兼容。
- 账号自己的 `accounts.node_id` 不受影响；新增账号仍需指定登录节点，编辑账号可更换登录节点。

## 升级说明

先升级框架到 v2.0.3，再停止插件并完整备份 `config.yaml` 与 `data` 目录。替换程序文件后保留原配置和数据库，再启动 v1.0.6。旧数据库中的插件服务节点字段可保留为无效数据，不需要修改账号表中的节点归属。

外发包只包含压缩、去符号并封装为 V8 字节码的单体运行程序及必要部署文件；不包含 TypeScript/Vue 源码、源码映射、`src`、`dist`、`node_modules`、独立 Node.js 环境、数据库、运行配置、日志、密钥或 Git 元数据。

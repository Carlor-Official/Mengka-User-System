# 萌卡用户系统插件 v1.0.7

本版本配合萌卡 NT v2.0.4，重新生成并审计三平台外发包。

## 调整

- 更新框架兼容基线到萌卡 NT v2.0.4。
- 保持插件服务与账号节点解耦，账号 API 继续由框架按照 `self_id + client_type` 路由到账号自己的登录节点。
- 沿用当前管理 API 契约，不恢复服务级 `node_id`、`all_nodes`、`system_management` 或旧 action 兼容分支。
- 外发包继续使用生产压缩、源码映射禁用和 V8 字节码单体封装，并在每个平台打包后执行源码防泄漏审计。

## 升级说明

先升级框架到 v2.0.4，再停止插件并完整备份 `config.yaml` 与 `data` 目录。替换程序文件后保留原配置和数据库，再启动 v1.0.7。

外发包只包含压缩、去符号并封装为 V8 字节码的单体运行程序及必要部署文件；不包含 TypeScript/Vue 源码、源码映射、`src`、`dist`、`node_modules`、独立 Node.js 环境、数据库、运行配置、日志、密钥或 Git 元数据。

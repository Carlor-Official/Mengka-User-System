# 萌卡用户系统插件 v2.0.0

## 必须先阅读的升级说明

这是不兼容 1.x 的重构版本。先升级萌卡 NT 框架至 **v2.0.5**，再将插件解压到独立新目录。

- 完整保留旧版 `config.yaml`、数据库、主密钥及整个 `data` 目录。不要把新版本数据库路径指向旧库。
- 使用全新 `data/user-system-v2.db`，不读取或迁移 1.x 数据；旧配置需要按新的示例重新填写。
- 管理员、用户采用独立账号密码和会话；首次启动创建本地管理员。框架 SSO、邮箱注册/找回、扫码归属及旧商品权益接口已删除。
- 一个服务令牌连接即可跨节点管理账号，不再按节点配置 WebSocket。
- 管理员先配置节点与协议绑定，再供用户选择登录协议；未绑定时不开放新的用户登录，已有在线会话不会因此中断。
- 卡密只支持付费套餐。备份必须同时包含数据库和主密钥；旧 2.0.0 测试卡若只保存哈希，仍能兑换，但无法恢复完整卡号或重新下载。

## 功能与流程

- 重构用户、套餐档位、订阅、卡密、订单、支付、公告、账号、等级任务、日志、会话及设置。
- 用户端简化为首页、账号、交易和设置；管理端日志分为用户、交易、支付，设置分为站点与客服。
- 修复同一 QQ 被其他用户登记后无法添加的问题：添加只登记 QQ，不自动创建或登录框架账号。明确提交登录配置时，允许覆盖同 QQ、同协议的原配置。
- 账号登记和运行控制分离；原用户套餐与订单不转移，旧控制者自动任务停用，非当前控制者删除登记不删除框架账号。Android 与 Linux 分别管理。
- 登录使用框架原生缓存、二维码、短信、滑块和身份验证流程，QQ 密码只交由框架保存。
- 协议支持按节点添加、编辑和删除绑定；卡密恢复列表、搜索、复制、分页、批次记录、自动下载和再次下载。
- 公告支持弹窗提醒间隔、站内信、未读提示、全部已读及个人清除已读。
- 等级任务黑名单只需 QQ 与原因，对该 QQ 各协议统一生效。
- 支付校验签名、金额与渠道流水，回调幂等；卡密加密保存，下载仅允许管理员访问。

## 界面修复

- 修复管理端账号、指纹、节点等页面的运行时空白问题，避免非账号数据误入账号计算与分页初始化错误。
- 统一卡片宽高、间距、表单底部按钮、移动端导航和易读日志。
- 清理重复按钮描边与双框选择器，恢复框架主题的玻璃层次、悬停与按压反馈。
- 修复弹窗下拉选项被遮挡、隐藏原生控件撑出滚动、颜色选择整行误触发和手机端复选框拉长。

## 安装

下载对应 Windows AMD64、Linux AMD64 或 Linux ARM64 包。复制示例配置后，Windows 运行 `scripts/start-windows.cmd`，Linux 运行 `bash scripts/start-linux.sh`，无需安装 Node.js。公网开放前完成首次管理员创建并配置 HTTPS。

服务端采用 V8 字节码封装，前端压缩并关闭 source map，敏感配置及卡密使用本地主密钥加密。客户端保护不能保证绝对不可逆向，请勿把真实配置、数据库或主密钥公开分享。

# 萌卡用户系统插件 v2.0.1

本次更新补齐用户端账号登录功能，继续配合萌卡 NT **v2.0.5** 使用。

## 新增与修复

- 已配置账号离线后可直接登录，不再每次要求填写密码、协议、设备和节点，无需删除账号重新添加。
- 账号更多操作新增“缓存登录”“重新登录”和“清理登录缓存”。普通登录优先复用已启用的缓存；缓存缺失或明确失效时再进入密码或扫码登录。
- “重新登录”可主动跳过缓存，继续使用已有账号配置；Android 与 Linux 登录继续支持框架原生验证流程。
- 接入框架全局缓存登录与自动登录设置。管理员统一设置，用户端可查看状态；关闭缓存登录时同步关闭自动登录。自动登录保持框架重启后通过缓存恢复上线的原有逻辑。
- 账号状态自动刷新。框架断连时保留已有列表并提示错误，避免误报为需要重新配置；框架记录消失后，可在原登记上重新配置，保留账号及套餐关联。
- 统一缓存登录、重新登录和二维码刷新的账号归属、节点、协议与套餐检查，修复异常响应可能被误报为登录成功的问题。
- 优化桌面与手机端登录操作菜单、等待反馈及扫码弹窗。

## 升级说明

**从 2.0.0 更新：**

1. 停止插件，完整备份 `config.yaml` 和整个 `data` 目录，包括数据库及 `master.key`。
2. 下载对应系统架构的 2.0.1 包，替换程序与启动脚本，保留原配置、数据库和主密钥；无需重新添加账号或购买套餐。
3. 启动插件，在账号页检查登录状态。需要修改全局缓存登录、自动登录时，由管理员操作。

**从 1.x 升级：**仍须使用独立新目录和全新 `data/user-system-v2.db`，不读取或迁移 1.x 数据；请先阅读 [2.0.0 升级说明](https://github.com/Carlor-Official/Mengka-User-System/releases/tag/v2.0.0)。

Windows 运行 `scripts/start-windows.cmd`；Linux 运行 `bash scripts/start-linux.sh`，无需安装 Node.js 或 npm。

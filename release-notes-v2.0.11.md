# 萌卡NT用户系统 2.0.11

- 优化首页加载与菜单切换：中文采用系统字体，减少大体积字体下载；复用首次身份校验结果，站内切换即时响应，保留服务端权限校验和会话失效检测。
- 减少首页统计前的重复身份请求，账号状态和套餐数据仍实时读取。
- 用户门户支持一键开启，默认使用框架对外地址加 `/user/`，无需填写端口、域名或配置额外反向代理。
- 保存门户配置后由框架热重启插件，页面显示重启进度和结果；无需重启框架。
- 自定义域名、HTTP 直连及反向代理移入高级设置，保留已有配置，支持切回默认门户。
- 修复 HTTPS 反向代理下正常注册、登录请求被误判为“请求来源无效”的问题，继续拒绝非法及跨站请求。
- 适配子路径下的登录、页面刷新、静态资源、验证码及支付跳转；用户门户使用独立会话，管理接口、菜单和资源由服务端隔离。

## 升级说明

一键同域门户及托管热重启需配合萌卡 NT **v2.1.8 或更新版本**。先更新框架，再更新本插件；从框架对外可访问的地址打开管理入口，在“设置 → 站点 → 用户门户”点击“一键开启用户门户”，成功后复制链接分享给用户。该入口复用框架网络访问条件。

框架 v2.1.8 同时修复更新和重启时残留插件进程、隔离身份占用及管理端不可达的问题；仅更新插件不能获得框架生命周期修复。已有独立门户配置继续保留，切换“跟随框架地址”后保存才迁移。

保留原有 2.x 配置、数据库和主密钥，无需重新登记账号或购买套餐。手动升级前停止插件并备份数据。托管在线更新仍需相应版本完成插件市场审核。

提供 Windows AMD64、Linux AMD64、Linux ARM64 独立部署包及对应托管包。


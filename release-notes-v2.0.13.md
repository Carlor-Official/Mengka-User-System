# 萌卡NT用户系统 v2.0.13

- 修复门户经过反向代理缓存后，账号已离线但列表仍显示在线、刷新状态滞后的问题。
- 加强身份、账号和个人数据接口的防缓存控制，避免错误共享缓存导致不同用户看到相同的身份或账号列表。
- 修复左上角站点标题刷新时先显示默认名称、再切换到自定义名称的闪动；登录页、用户端和管理端首屏直接显示保存的标题。

## 升级说明

保留原有 2.x 配置、数据库和主密钥，无需删除账号或重新购买套餐。

**使用宝塔或 Nginx 自定义域名反向代理的用户，请关闭门户整站缓存。** 门户包含登录身份与实时数据，不能按 URL 共享缓存。删除 `proxy_ignore_headers Set-Cookie Cache-Control expires;` 等忽略上游缓存控制的配置，并使用 `proxy_cache off;`。仅升级插件不能替代修正强制缓存规则。

推荐配合萌卡 NT v2.1.8 或更新版本使用。独立部署可从管理端在线更新；托管在线更新需本版本完成插件市场审核。手动升级前停止插件并完整备份数据。

提供 Windows AMD64、Linux AMD64、Linux ARM64 独立部署包及对应 managed 托管包。

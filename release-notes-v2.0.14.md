# 萌卡NT用户系统 v2.0.14

- 修复 Linux 原生数据库模块依赖过高，导致旧系统启动时报 `GLIBC_2.38 not found` 或直接退出的问题；Linux AMD64 / ARM64 按 GLIBC 2.28 基线构建。
- 适配框架 v2.2.0 托管方式，启动时自动读取框架分配的连接、端口、凭据与数据目录。
- 简化用户门户为一键开关和分享地址，默认通过框架对外地址加 `/user/` 访问；保存后由框架热重启生效，移除插件内自定义 SSL、域名、端口和反向代理配置。
- 门户与支付回调跟随框架可信地址；加强用户与管理员入口隔离，保留用户数据归属及防缓存保护。
- 加强框架连接握手、心跳检测与断线恢复，同步最新等级任务规则并减少等级页重复请求。

## 升级说明

**托管部署请先升级萌卡 NT 至 v2.2.0 或更新版本，再升级插件。** 从框架进入的插件管理会话跟随当前设备的框架登录；刷新访问令牌不再导致后台退出。该行为由框架 v2.2.0 提供，退出框架或撤销登录仍会失效。

保留原有 2.x 配置、数据库与主密钥，无需删除账号或重新购买套餐。升级前请备份业务数据。旧独立门户配置会备份后转换为框架入口；旧端口和旧自定义域名不会继续提供门户，请重新复制并分享框架生成的 `/user/` 地址。

Linux 支持 GLIBC 2.28 及以上，并需系统标准 C++ 运行库；不包含 musl/Alpine 或 GLIBC 2.28 以下系统。无需手工替换系统 GLIBC。

提供 Windows AMD64、Linux AMD64、Linux ARM64 独立部署包及对应 managed 托管包。独立部署可通过管理端检查 GitHub 更新；托管安装和在线更新仍需本版本完成插件市场审核。

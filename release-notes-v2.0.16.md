# 萌卡 NT 用户系统 v2.0.16

> 此版本曾被错误标记为 `v2.1.0`，现已更正为 `v2.0.16`。原附件中的版本文件名保持不变，仅用于历史版本追溯。

- 在线更新入口与交互调整为框架同款：管理端右上角检查更新，显示版本对比、GitHub 更新说明、安装进度及失败原因，并支持每日自动检查和“稍后更新”。
- 独立部署采用 GitHub 直连、`gh-proxy.com`、`ghproxy.net` 三条下载线路并发测速，自动选择可用节点；下载失败时按节点降级重试，并继续校验文件大小、SHA256 和安装包内容。
- 框架托管部署继续由框架网关下载、替换、回滚和重启；配合萌卡 NT v2.3.3 及以上可使用相同的更新节点探测与降级逻辑。
- 卡密管理新增当前页多选、批量复制、批量删除及一键删除全部未使用卡密；删除操作带确认提示、管理员鉴权和审计记录。
- 管理端在线更新代码和卡密批量接口继续与普通用户门户产物隔离，普通用户无法加载菜单或调用管理接口。

## 升级说明

托管部署最低要求萌卡 NT v2.2.0；如需框架同款多节点在线更新，请升级到萌卡 NT v2.3.3 或更新版本。保留原有 2.x 配置、数据库及主密钥，无需重新添加账号、卡密或套餐；升级前请备份业务数据。

Linux AMD64 / ARM64 支持 GLIBC 2.28 及以上，需系统标准 C++ 运行库，不包含 musl/Alpine。

# 萌卡 NT 用户系统 2.0.17 原生独立外发包

## 发布内容

- 用户系统作为独立插件外发包发布，不包含在萌卡 NT 框架发行包内。
- 支持框架 `2.4.1` 及以上版本的 `native-ipc-v1` 接入。
- 保留正向与反向 WebSocket 兼容接入方式。
- 插件由用户从本 Release 下载后，在框架「插件 → 插件导入」中手动上传、校验并运行。

## 下载与校验

请从本 Release 的附件下载 Linux AMD64 成品包、包清单和 `SHA256SUMS.txt`，安装前核对校验值。框架不会从官网自动下载或更新该插件。


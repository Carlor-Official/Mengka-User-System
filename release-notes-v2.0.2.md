# 2.0.2

从 2.0.0 / 2.0.1 更新时，先停止插件并备份 `config.yaml` 和整个 `data` 目录，再替换程序与启动脚本，保留原数据库和主密钥。需先将萌卡 NT 升级到 2.0.9 或更新版本；从 1.x 升级仍须独立新目录和全新数据库。

- 等级加速接入框架 2.0.9 共享管理 API，开关、计划、任务标记、缓存和等级进度与框架使用同一数据源。
- 停用插件自身等级任务定时器；旧计划保留供核对，不自动覆盖框架已有设置。执行后刷新失败明确提示，不自动重试。
- 等级操作在服务端检查账号归属、可用套餐和任务黑名单，普通用户无法读取其他账号的计划与数据。
- 合并管理员和普通用户的登录入口，不再手动选择登录身份；根据账号的实际权限进入相应控制台。
- 页面地址统一为 `/overview`、`/accounts` 等路径，不再按 `/user`、`/admin` 分开。升级后请从 `/login` 重新登录并更新书签。
- 两种账号均支持“保存密码”和“30 天保持登录”。密码由浏览器密码管理器保存、填充；保持登录由服务端管理，进程重启后仍可恢复有效会话。
- 加强管理员菜单、页面资源和接口的权限隔离，普通用户通过修改地址、Cookie 或请求参数也无法取得管理员内容。
- 退出或切换账号时同步刷新其他标签页；返回历史页面会重新检查身份，避免继续显示上一账号的内容。
- 加强会话撤销、登录尝试限制与请求来源校验，修复支付返回页的脚本注入风险。
- 保留 2.0.1 的缓存登录、重新登录和框架全局自动登录逻辑。

HTTPS 生产环境请启用 `session.secureCookie`，通过可信反向代理提供 HTTPS 时正确配置 `server.trustProxy`。未勾选保持登录时使用浏览器会话 Cookie；主动退出会立即撤销服务端会话。


等级计划升级：旧插件计划不再执行，请核对框架现有计划后按需保存。账号、套餐、数据库和主密钥请保留。

# 萌卡NT用户系统 2.0.5

- 同时支持独立部署的正向 / 反向 WebSocket 和插件市场自动部署。
- 市场安装后自动连接框架，管理员可从框架快捷进入插件后台完成初始化。
- 管理入口及用户门户支持 HTTP、HTTPS、IP 和域名；直连模式可选择监听端口，并检查端口占用。
- 管理员入口与普通用户门户保持隔离，托管连接信息由框架管理。
- 账号恢复与等级任务继续复用框架接口，提供标准 SDK 接入说明和示例。

## 升级说明

停止插件并备份数据库、主密钥及配置后升级。2.x 用户保留原数据目录；1.x 数据库不能直接作为 2.x 数据库使用。

独立部署：从 config.example.yaml 创建自己的配置，在 framework.mode 选择 forward 或 reverse，填写地址、端口和令牌。

市场部署：使用支持新版访问配置的萌卡 NT 框架，由框架分配连接和管理员入口；用户门户在插件“设置 → 站点”中单独启用和配置。

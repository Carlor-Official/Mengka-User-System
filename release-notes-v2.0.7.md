# 萌卡NT用户系统 2.0.7

- 用户门户支持即时启停：勾选后点击“保存并启用”即可开放，关闭立即停止公网监听，无需重启插件，管理员入口继续可用。
- 简化门户设置，默认自动填写 HTTP 直连地址，将端口、域名和反向代理选项收进“访问设置”；已有代理配置继续保留。
- 保存成功、端口占用和启动失败均在门户卡片内提示；保存期间禁用重复提交，运行状态随实际监听更新。
- 换端口或切换访问方式失败时保留原门户和配置；HTTP / HTTPS 切换立即更新登录 Cookie 设置。
- 保留普通用户门户与管理员接口、页面资源及框架托管凭据的隔离。

## 升级说明

停止插件并备份配置、数据库和主密钥后升级，保留原有 2.x 数据目录，无需重新添加账号。1.x 数据库不能直接用于 2.x。安装此版本后，后续门户设置修改无需重启插件。

提供 Windows AMD64、Linux AMD64、Linux ARM64 独立部署包及插件市场托管包。市场安装请选择名称含 `managed` 的 ZIP，推荐配合萌卡 NT 2.1.1 或更新版本。

HTTP 直连需要服务器防火墙或安全组放行门户端口，域名解析到服务器；HTTPS 继续使用反向代理和对应证书。管理员仍从框架打开管理入口，普通用户使用独立门户地址。

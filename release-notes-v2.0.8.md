# 萌卡NT用户系统 2.0.8

- 管理端新增站点标题设置，保存后同步登录页、控制台名称及浏览器标签，重启后保留。

- 用户账号列表补充套餐名称和到期时间，桌面与手机均可查看；多套餐分别显示，区分有效、已到期、已撤销和免费长期套餐，续费后自动刷新。

- 修复账号状态同步失败后仍显示绿色在线和历史“登录成功”的问题，失联显示“状态待同步”，恢复连接后重新同步框架状态。
- 等级任务页单独显示并同步框架账号状态，任务接口 W21 不再与账号整体离线混淆；同 QQ 的 Android、Linux 状态保持独立。
- 同 QQ 的 Android 账号在线时，Linux 可选择授权免扫码登录；普通用户必须拥有对应账号的控制权。
- 修复 HTTP 门户中复制验证链接无效的问题，增加复制失败提示和手动复制方式。
- 账号登录的运行节点只显示节点名称，隐藏代理类型、IP 和端口。
- 新增管理员在线更新入口，对接 GitHub 正式发布页，支持版本检查、更新说明、程序校验和更新重启；启动失败时保留恢复旧程序的能力。

## 升级说明

首次安装本版本需停止插件并备份配置、数据库和主密钥后手动升级。保留原有 2.x 数据目录，无需重新添加账号；1.x 数据库不能直接使用。

独立部署后续可在线更新，安装目录须对服务账号可写。Linux systemd 用户应同步新版 unit 的 `ReadWritePaths` 并重载服务配置。程序回滚不会自动回退业务数据库。

托管在线更新需要包含托管升级接口的配套框架；已发布的框架 v2.1.4 尚不含该能力，此时请继续使用原有手动升级方式。托管更新同时核对 GitHub 发布与插件市场审核清单，等待两者就绪后可安装。

提供 Windows AMD64、Linux AMD64、Linux ARM64 独立部署包及对应托管包。

# 萌卡NT用户系统 v2.1.1

本次仅更新插件，适配官方萌卡 NT **v2.4.4 或更新版本**，无需定制或修改框架。

- 用户管理支持分页、全库搜索和真实总数，修复只能显示 200 位用户的问题。
- 修复已完成等级任务显示“暂不支持”、部分等级数据和状态展示不一致；计划和实际执行继续由框架负责。
- 小说等现有长任务通过插件后台执行并查询进度，减少页面等待超时，不自动重放失败或中断的操作。
- 修复账号列表刷新跳动，加强账号归属校验，阻止普通用户接管其他用户的账号。
- 改善 Linux 免扫码流程与登录验证状态判断；只有框架确认上线才显示成功。
- 修复授权到期时间的时区问题、有效套餐选择及续费同步竞态。
- 删除账号前处理相关自动计划，删除后核实框架状态，避免未确认删除时误报成功。
- 托管用户门户统一使用框架对外地址加 `/user/`，开关保存立即生效；登录、等级页面、修改密码与支付返回路径保持一致。

## 升级说明

1. 先确认框架为官方 **v2.4.4 或更新版本**，停止插件并备份配置、数据库和主密钥。
2. 下载与系统架构匹配的 `managed-native` 包，在框架「插件 → 插件导入」中更新。保留原 2.x 数据目录，不需要重新购买套餐或添加账号。
3. 用户门户改为框架对外地址下的 `/user/`。原插件自定义域名、端口、SSL 和反向代理设置不再使用；旧网络配置自动备份为 `config.before-framework-network.yaml`，请向用户分发新入口。
4. 框架已在执行的任务不能由插件原子取消；删除前应等待相关任务结束。后台进度记录在插件重启后失效，应刷新 QQ 状态核对，不要据此重复提交。

提供 Windows AMD64、Linux AMD64 和 Linux ARM64 三个平台的原生 IPC 包。Linux 保持 GLIBC 2.28 兼容基线。1.x 数据库不能直接迁入。

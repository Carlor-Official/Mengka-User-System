# 萌卡NT用户系统 v2.1.2

新增代理等级、商品折扣和补差价升级功能，继续使用官方框架 v2.4.4 或更新版本，仅更新插件。

- 管理端新增独立「代理等级」页面，可配置等级名称、序号、商品折扣率、升级总价及开放购买状态。
- 用户管理显示代理等级，并支持管理员手动分配等级或恢复为普通用户。
- 用户从头像菜单「个人信息 → 代理等级」查看权益、升级等级；已有代理按已计入升级金额补差价。例如一级已付 100 元、二级总价 250 元，升级只需 150 元。
- 套餐购买和续费自动按代理折扣结算，同时显示原价和代理价。代理升级费不参与折扣，卡密兑换不重复折扣。
- 付款确认后自动提升等级；重复提交复用待支付订单，重复回调不会重复开通，修改价格不改变旧订单金额。
- 代理继续使用普通用户权限，不能访问管理端页面、菜单或管理接口。

## 升级说明

1. 在框架停止插件，完整备份原配置、数据库与主密钥，然后导入对应平台的新安装包。保留原数据目录，启动时自动增量扩展数据库。
2. 在管理端「代理等级」创建等级。折扣率 100% 为原价、80% 为八折；升级价格填写目标等级总价。
3. 配置现有在线支付渠道后，用户即可自行付费升级。管理员手动分配不收款，升级抵扣金额设为所选等级当前总价；恢复普通用户会清除抵扣金额。
4. 关闭某等级购买只停止新升级，已获得的身份与折扣继续有效。已有用户或历史订单关联的等级不能删除；有待支付升级订单时，先完成该订单再调整身份或更换升级等级。

提供 Windows AMD64、Linux AMD64、Linux ARM64 原生 IPC 安装包。Linux 继续要求 GLIBC 2.28 及以上和标准 C++ 运行库；用户门户继续使用框架地址下的 `/user/`。
